<div class="mt-4">
  <form action="/favourites" method="post">
    <button
      type="submit"
      class="animate-bounce bg-gradient-to-r from-pink-400 to-blue-500 hover:from-blue-500 hover:to-blue-600 text-white font-bold py-2 px-4 rounded-full transition duration-300 ease-in-out transform hover:scale-105"
    >
      Add To favourite
    </button>
    <input type="hidden" name="id" value="<%= home._id %>" />
  </form>
</div>
